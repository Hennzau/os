# The QMK userspace (keymaps) as shipped, copied once to where wheel can
# edit and build it; later images do not overwrite it.
C  /var/lib/qmk 0775 root wheel - /usr/share/qmk/userspace
A+ /var/lib/qmk -    -    -     - d:g:wheel:rwX,g:wheel:rwX
