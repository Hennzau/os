# The firewall's ruleset, and the directory for local rules it includes.
L /etc/nftables.conf
d /etc/nftables.d 0755 root root -

# Our ssh client defaults. ssh_config itself, which includes ssh_config.d,
# openssh's own tmpfiles copy in.
L /etc/ssh/ssh_config.d/50-elvos.conf
