# elvos-vpn-watch owns the routing policy rule that sends traffic into a
# tunnel - that is how it switches between them. networkd removes rules it
# did not write itself unless told otherwise, and it would take that one
# with it on every reconfigure.
[Network]
ManageForeignRoutingPolicyRules=no
