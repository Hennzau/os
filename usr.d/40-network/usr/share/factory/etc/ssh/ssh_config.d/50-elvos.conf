# Keys, once used, go to the agent (ssh-agent.socket, per user); an idle
# connection is probed every minute instead of being dropped by some NAT.
Host *
    AddKeysToAgent yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
