# containers-common's policy.json, registries.conf.d and friends, with our
# storage.conf - copied once, not linked: rootful podman writes its networks
# into /etc/containers/networks.
C /etc/containers
d /var/lib/containers/users 0755 root root -
