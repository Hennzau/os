# Swap, encrypted like the root (see 40-root.conf). Its weight is the
# default, 1000, against root's and /home's much larger ones: it gets its
# minimum on most disks.
[Partition]
Type=swap
Format=swap
SizeMinBytes=4G
SizeMaxBytes=32G
Encrypt=tpm2
FactoryReset=yes
Label=%M-swap
