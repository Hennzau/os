# The A slot: the partitions the image was built with. repart matches existing
# partitions to definitions by type, in order, so these must come first or the
# B definitions below would claim - and resize - the A partitions instead.
[Partition]
Type=usr-verity-sig
