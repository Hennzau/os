# /home, not encrypted as a whole: systemd-homed encrypts each home itself,
# a LUKS image per user. Twice the root's weight: most of the disk.
[Partition]
Type=home
Format=btrfs
SizeMinBytes=1G
Weight=40000
FactoryReset=yes
Label=%M-home
