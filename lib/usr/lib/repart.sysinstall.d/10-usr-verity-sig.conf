# auto: the partition backing the running /usr, or its verity partitions. The
# labels are what systemd.image_filter selects on at boot.
[Partition]
Type=usr-verity-sig
Label=%M_%A_verity_sig
CopyBlocks=auto
